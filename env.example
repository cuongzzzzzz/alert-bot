# Server Monitoring Bot Configuration

# REQUIRED: Lark/Feishu webhook URL for sending alerts
# Get this from your Lark bot configuration
LARK_WEBHOOK_URL=https://open.larksuite.com/open-apis/bot/v2/hook/ef3b37ce-9362-4c79-a004-171f2e54d28e

# Server URLs to monitor (comma-separated)
# Example: https://api.example.com,https://app.example.com,https://status.example.com
SERVER_URLS=https://httpbin.org/status/200,https://jsonplaceholder.typicode.com/posts/1,https://api.github.com

# Monitoring interval (cron format)
# Default: * * * * * (every minute)
# Examples:
#   */5 * * * * = every 5 minutes
#   0 */1 * * * = every hour
#   0 0 */6 * * = every 6 hours
MONITORING_INTERVAL=* * * * *

# HTTP request timeout in milliseconds (default: 10000 = 10 seconds)
REQUEST_TIMEOUT=10000

# Number of retry attempts for failed requests (default: 3)
RETRY_ATTEMPTS=3

# Delay between retry attempts in milliseconds (default: 1000 = 1 second)
RETRY_DELAY=1000

# HTTP status codes considered as "server running" (comma-separated)
# Default: 200,201,202,204,301,302,304
SUCCESS_STATUS_CODES=200,201,202,204,301,302,304

# Enable verbose logging (true/false)
# When true, logs successful health checks with response times
VERBOSE_LOGGING=false

# Send recovery notifications when servers come back online (true/false)
# Default: true
SEND_RECOVERY_NOTIFICATIONS=true
